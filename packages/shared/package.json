{
  "name": "@vibe-coder/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared utilities and types for Vibe Coder",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^0.34.6",
    "@vitest/coverage-v8": "^0.34.6"
  },
  "dependencies": {
    "zod": "^3.22.4"
  }
}